# os -> included by default in python
# json -> included by default in python

# --- Export-only dependencies (not needed for training/inference) ---
# onnx
# onnxruntime
# # onnxruntime-gpu  # Optional: for FP8 support on NVIDIA GPU
# gguf>=0.10.0

# --- Core ---
torch
# numpy==1.26  -> python 3.12
numpy
python-aiml
transformers
datasets
psutil

# --- Tokenizer ---
sentencepiece
huggingface_hub

# --- Data sources ---
pypdf
ebooklib
trafilatura
beautifulsoup4
requests

# --- API server ---
fastapi
uvicorn
pydantic

# --- Optional: ESC-to-stop training (requires display server, not headless) ---
# keyboard
